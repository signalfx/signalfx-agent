pipeline: mesosphere

plugins:
    mesosphere:
        plugin: observers/mesosphere

    collectd:
        staticPlugins:
            mesos:
                plugin: mesos-agent

    integrations:
        overrides: zk://signalfx/agent/integrations

pipelines:
    mesosphere:
    - mesosphere
    - integrations
    - proxy
    - collectd
