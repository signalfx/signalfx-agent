pipeline: mesosphere

plugins:
    mesosphere:
        plugin: observers/mesosphere

    collectd:
        staticPlugins:
            mesos:
                plugin: mesos-agent

pipelines:
    mesosphere:
    - mesosphere
    - integrations
    - proxy
    - collectd
