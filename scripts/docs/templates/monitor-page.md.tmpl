<!--- GENERATED BY gomplate from scripts/docs/monitor-page.md.tmpl --->
{{ with ds "monitor" }}
# {{.monitorType}}
{{with .doc}}
{{.}}
{{- end }}

**Accepts Endpoints**: {{if .acceptsEndpoints}}**Yes**{{else}}No{{end}}

**Only One Instance Allowed**: {{if .singleInstance}}**Yes**{{else}}No{{end}}

## Configuration

| Config option | Default | Required | Type | Description |
| --- | --- | --- | --- | --- |
{{ range .fields -}}
| `{{.yamlName}}` | {{if .default}}`{{.default}}`{{end}} | {{if .required}}**yes**{{else}}no{{end}} | `{{.type}}` | {{.doc | strings.TrimSpace}} |
{{end}}

{{ range .fields -}}
{{ if (has . "elementStruct") -}}
The `{{.yamlName}}` config object has the following fields:

| Config option | Default | Required | Type | Description |
| --- | --- | --- | --- | --- |
{{ range .elementStruct.fields -}}
| `{{.yamlName}}` | {{if .default}}{{.default}}{{end}} | {{if .required}}**yes**{{else}}no{{end}} | `{{.type}}` | {{.doc | strings.TrimSpace}} |
{{end}}
{{- end }}
{{end}}

{{with .metrics -}}
## Metrics

| Name | Type | Description |
| ---  | ---  | ---         |
{{ range . -}}
| {{.name}} | {{.type}} | {{.description | strings.TrimSpace}} |
{{end}}
{{end -}}

{{with .dimensions -}}
## Dimensions

| Name | Description |
| ---  | ---         |
{{ range . -}}
| {{.name}} | {{.description | strings.TrimSpace}} |
{{end}}
{{end -}}

{{with .properties -}}
## Properties

| Name | Dimension | Description |
| ---  | ---       | ---         |
{{ range . -}}
| {{.name}} | {{.dimension}} | {{.description | strings.TrimSpace}} |
{{end}}
{{end}}
{{end}}
