#!/bin/bash

# Adds a file from the output dir to a Debian package repo.  We use the same
# package for every distro, but offer separate dirs for each in case we decide
# to have distro-specific packages in the future.
# This will sign the repo Release file unless the envvar NO_SIGN is non-empty.

set -euxo pipefail

distros="wheezy jessie stretch trusty xenial generic"

GPG_KEY_ID=${GPG_KEY_ID:-185894C15AE495F6}
OUTPUT_DIR=${OUTPUT_DIR:-/output}
REPO_TYPE=${REPO_TYPE:-test}


case $REPO_TYPE in
  main|beta|test) ;;
  *) echo "REPO_TYPE must be 'main', 'beta' or 'test'" >&2 && exit 1 ;;
esac

repo_dir="/repo/${REPO_TYPE}"
pool_dir="${repo_dir}/pool"
s3_bucket="s3://signalfx-agent-test-packages/debs/signalfx-agent/$REPO_TYPE"

mkdir -p $repo_dir
aws s3 sync --delete $s3_bucket $repo_dir

mkdir -p $pool_dir
for f in $(find $OUTPUT_DIR -name "*.deb"); do
  cp -n $f $pool_dir
  if [ $? -ne 0 ]; then
    echo "Debian package $(basename $f) already exists in this repo.  If you want to overwrite
    it, please delete it from the repo first." >&2 && exit 2
  fi
done

cd $repo_dir
for distro in $distros; do
  mkdir -p $distro
  apt-ftparchive packages pool > $distro/Packages
  gzip -k $distro/Packages
  xz -k $distro/Packages

  apt-ftparchive release $distro > $distro/Release

  if [[ -z ${NO_SIGN-} ]]; then
    gpg --default-key $GPG_KEY_ID --digest-algo SHA256 -abs -o $distro/Release.gpg $distro/Release
    gpg --default-key $GPG_KEY_ID --digest-algo SHA256 --clearsign --output $distro/InRelease $distro/Release
  fi
done

aws s3 sync --dryrun $repo_dir $s3_bucket

read -p "Do the above file updates to the s3 bucket seem correct? [y/N] "
[[ ! $REPLY =~ ^[Yy]$ ]] && echo "Cancelling upload" && exit 1

aws s3 sync $repo_dir $s3_bucket
