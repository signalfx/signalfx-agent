<!--- GENERATED BY gomplate from the scripts/docs/monitor.tmpl --->

{{ $monitors := (ds "agent").Monitors }}

# Monitor Configuration

These are all of the monitors included in the agent along with their possible
configuration options:

{{ range $monitors -}}
[{{.monitorType}}]({{strings.ReplaceAll "/" "-" (strings.ToLower .monitorType)}})
{{end}}

{{ range $monitors -}}
## {{.monitorType}}
{{with .doc}}
{{.}}
{{- end }}
| Config option | Default | Required | Type | Description |
| --- | --- | --- | --- | --- |
{{ range .fields -}}
| {{.yamlName}} | {{.default}} | {{.required}} | {{.type}} | {{.doc}} |
{{end}}
{{end}}
