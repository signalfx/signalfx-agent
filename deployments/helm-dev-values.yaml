# A Kubernetes Helm values file that you can use when deploying the agent in a
# development K8s cluster.  You should change the imageTag to whatever tag you
# are using for development.
signalFxAccessToken: MYACCESSTOKEN
clusterName: dev-cluster
imageRepository: quay.io/signalfx/signalfx-agent-dev
imageTag: 3.0.0
logLevel: debug
pullPolicy: Always
kubeletAPI:
  skipVerify: true
