{{define "mongodb"}}
LoadPlugin python
<Plugin "python">
  ModulePath "{{Globals.PluginRoot}}/mongodb-collectd-plugin"
  Import "mongodb"
  {{range $instance := .}}
  	{{RenderTemplate $instance.Template $instance}}
  {{end}}
</Plugin>
{{end}}
