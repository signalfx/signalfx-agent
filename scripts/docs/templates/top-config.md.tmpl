<!--- GENERATED BY gomplate from scripts/docs/top-config.md.tmpl --->
# Agent Configuration

The agent is configured primarily by a YAML document conventionally located at
`/etc/signalfx/agent.yaml`.  The location of the config file can be specified
by the `-config` flag to the agent binary (`signalfx-agent`).

## Config Schema
{{ with (ds "agent").TopConfig }}
{{ $doc_types := (ds "agent").doc_types -}}
| Config option | Default | Required | Type | Description |
| --- | --- | --- | --- | --- |
{{ range .fields -}}
| `{{.yamlName}}` | {{if .default | print}}`{{.default | data.ToJSON}}`{{end}} | {{if .required}}**yes**{{else}}no{{end}} | {{if has . "elementStruct"}}[{{end}}`{{or (index $doc_types .type) .type}}{{if .elementKind}} of {{or (index $doc_types .elementKind) .elementKind}}{{end}}`{{if has . "elementStruct"}}](#{{.yamlName | toLower}}){{end}} | {{.doc | strings.TrimSpace}} |
{{end}}

{{ range .fields -}}
{{ if (has . "elementStruct") -}}
## {{.yamlName}}
The **nested** `{{.yamlName}}` config object has the following fields:
{{ if eq .yamlName "monitors" }}
For more information on monitor configuration, see [Monitor Config](./monitor-config.md).
{{end -}}
{{ if eq .yamlName "observers" }}
For more information on observer configuration, see [Observer Config](./observer-config.md).
{{end}}

| Config option | Default | Required | Type | Description |
| --- | --- | --- | --- | --- |
{{ range .elementStruct.fields -}}
| `{{.yamlName}}` | {{if .default | print}}`{{.default}}`{{end}} | {{if .required}}**yes**{{else}}no{{end}} | `{{or (index $doc_types .type) .type}}{{if .elementKind}} of {{or (index $doc_types .elementKind) .elementKind}}{{end}}` | {{.doc | strings.TrimSpace}} |
{{end}}

{{end -}}
{{end -}}
{{end}}
