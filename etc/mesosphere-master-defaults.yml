pipeline: file

plugins:
  collectd:
    templates:
      overrides: zk://signalfx/agent/templates
    staticPlugins:
      marathon:
        plugin: marathon
      mesos:
        plugin: mesos-master

