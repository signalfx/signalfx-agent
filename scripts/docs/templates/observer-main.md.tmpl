<!--- GENERATED BY gomplate from scripts/docs/observer-main.md.tmpl --->
{{ $observers := (ds "agent").Observers }}
{{ $doc_types := (ds "agent").doc_types -}}
# Observer Configuration

Observers discover service endpoints that can then be monitored.  They
are configured in a list called `observers` in the [main agent config
file](./config-schema.md). These are all of the observers included in the agent
along with their possible configuration options:

{{ range $observers -}}
- [{{.observerType}}](./observers/{{strings.ReplaceAll "/" "-" (strings.ToLower .observerType)}}.md)
{{end}}

## Common Configuration

The following config options are common to all observers:

| Config option | Default | Required | Type | Description |
| --- | --- | --- | --- | --- |
{{ range (ds "agent").GenericObserverConfig.fields -}}
| `{{.yamlName}}` | {{if and (.default | conv.ToString) (ne (.default | conv.ToString) "nil")}}`{{.default}}`{{end}} | {{if .required}}**yes**{{else}}no{{end}} | `{{or (index $doc_types .type) .type}}{{if (eq .type "struct") }} (see below){{end}}{{if .elementKind}} of {{or (index $doc_types .elementKind) .elementKind}}{{if ne .elementKind "interface"}}s{{end}}{{if (eq .elementKind "struct") and (ne .elementKind "interface")}} (see below){{end}}{{end}}` | {{.doc | strings.TrimSpace}} |
{{end}}
