fixtures:
  forge_modules:
    stdlib:
      repo: "puppetlabs/stdlib"
      ref: "4.24.0"
    apt:
      repo: "puppetlabs/apt"
      ref: "7.0.0"
    archive:
      repo: "puppet/archive"
      ref: "4.1.0"
    powershell:
      repo: "puppetlabs/powershell"
      ref: "2.3.0"

